apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
  labels:
    app: "{{ .Release.Name }}"
spec:
  type: "{{ .Values.service.type }}"
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: "{{ .Release.Name }}"
